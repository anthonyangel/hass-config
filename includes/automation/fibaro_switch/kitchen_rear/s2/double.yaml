alias: Kitchen Rear Button 2 Double
trigger:
  platform: event
  event_type: zwave_mqtt.scene_activated
  event_data:
    node_id: "{{ state_attr('light.kitchen_rear', 'node_id') }}"
    scene_id: 24
action:
  - service: light.toggle
    entity_id: light.external_rear
