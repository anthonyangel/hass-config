alias: bathroom Button 2 Triple
trigger:
  platform: event
  event_type: zwave_mqtt.scene_activated
  event_data:
    node_id: "{{ state_attr('light.bathroom', 'node_id') }}"
    scene_id: 25
action:
  - service: light.toggle
    entity_id: light.stairs
