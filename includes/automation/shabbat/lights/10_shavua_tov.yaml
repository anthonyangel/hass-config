alias: Shavua Tov
trigger:
  - platform: state
    entity_id: binary_sensor.shabbat
    from: "on"
    to: "off"
action:
  - service: light.turn_on
    data:
      entity_id:
        - light.lamp
        - light.toy_kitchen_worktop
      brightness_pct: 100
      color_name: blue
  - service: light.turn_on
    entity_id:
      - light.hood_ambient_blue
