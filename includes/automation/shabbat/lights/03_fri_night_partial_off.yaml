alias: Friday Night Lamp Off
trigger:
  platform: time
  at: "19:30:00"
condition:
  condition: and
  conditions:
    - condition: state
      entity_id: 'binary_sensor.shabbat'
      state: 'on'
action:
  - service: light.turn_off
    entity_id:
      - light.stairs
